services:
  law:
    build: .
    container_name: law-app
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - DB_PATH=/data/law/law.db
    volumes:
      - /data/law:/data/law
    restart: unless-stopped
